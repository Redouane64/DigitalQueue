@model IEnumerable<User>

<table class="table">
    <thead>
    <tr>
        <th>Title</th>
        <th>Actions</th>
    </tr>
    </thead>
    <tbody>
    @if (Model is null || Model?.Count() == 0)
    {
        <tr>
            <td colspan="2">
                <i>No Users</i>
            </td> 
        </tr>
    }
    else
    {
        foreach (var user in Model)
        {
            <tr>
                <td>
                    @user.UserName
                </td> 
            </tr>
        }
    }
    </tbody>
</table>